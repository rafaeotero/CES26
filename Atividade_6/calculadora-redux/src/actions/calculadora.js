export const somar = (numero) => ({
    type: 'SOMAR',
    payload: numero,
  });
  
  export const subtrair = (numero) => ({
    type: 'SUBTRAIR',
    payload: numero,
  });
  
  export const multiplicar = (numero) => ({
    type: 'MULTIPLICAR',
    payload: numero,
  });
  
  export const dividir = (numero) => ({
    type: 'DIVIDIR',
    payload: numero,
  });
  